import ProfileInputText from '../../components/Authentication/ProfileInputText';
import styles from './index.module.css';

const ProfileInfo = () => {
  return (
    <div className={styles.App}>
      <ProfileInputText />
    </div>
  );
};

export default ProfileInfo;
